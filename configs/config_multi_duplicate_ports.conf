server {
    listen 80;
    root /var/www/html;

    location / {
        root /var/www/html;
        index index.html;
    }
}

server {
    listen 80;
    root /var/www/public;

    location /api {
        root /var/www/api;
        index index.php;
    }
}
