server {
    listen 80;
    root /var/www/html;

    location / {
        root /var/www/html;
        index index.html;
    }

    location /images {
        root /var/www/images;
        index index.png;
    }
}
